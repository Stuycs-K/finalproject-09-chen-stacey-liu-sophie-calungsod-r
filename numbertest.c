#include "numbertest.h"

int main(){

}

int findNumber(char* str){
  int num = 0;
  char * ptr = str;
  while (&ptr != '\n'){
    if (c == ' '){
      num << 1;
    }
    else if (c == '\t'){ // does it need to be ascii?
      num << 1;
      num+=;
    }
    ptr++;
  }
}
